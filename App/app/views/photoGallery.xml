<Alloy>
	<Window class="container">
		<Label id="loadPhoto" onClick="clickLabel">Click to fetch!</Label>
		<Label id="uploadPhoto" onClick="choosePhoto">Click to upload!</Label>
		<Label id="back" onClick="closeWindow">Back to Home</Label>
		<TableView id="tableView"></TableView>
	</Window>
</Alloy>